extends RefCounted
class_name GameTypes

# GameTypes.gd
# Global type definitions shared across the game.
# Add new enums here as universal concepts emerge (damage types, status effects, etc.)

enum Faction {
	PLAYER,
	ENEMY,
}
